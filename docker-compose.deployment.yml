version: '3.8'

# Deployment configuration with pre-built images
services:
  dcv-server:
    image: ghcr.io/your-org/dcv-server:latest
    # Remove build context in deployment

  nginx:
    image: ghcr.io/your-org/nginx-dcv-proxy:latest
    # Remove build context in deployment
